<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="description" content="Demo: SMS Consent Collection Flow - See how FilterFive-powered businesses collect SMS consent from customers">
  <title>Demo: SMS Consent Collection - FilterFive</title>
  <link rel="stylesheet" href="/css/homepage.css">
  <style>
    /* Demo Page Specific Styles */
    .demo-container {
      min-height: 100vh;
      background-color: var(--bg-light);
      padding: var(--spacing-lg) 0;
    }

    .demo-banner {
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      color: white;
      padding: var(--spacing-md) var(--spacing-sm);
      margin-bottom: var(--spacing-lg);
      border-radius: var(--border-radius);
      text-align: center;
    }

    .demo-banner p {
      font-size: 16px;
      margin: 0;
      font-weight: 500;
    }

    .demo-content-wrapper {
      max-width: 600px;
      margin: 0 auto;
      padding: 0 var(--spacing-sm);
    }

    /* Form Styles */
    .demo-form-card {
      background: var(--bg-white);
      border-radius: var(--border-radius);
      padding: var(--spacing-lg);
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      margin-bottom: var(--spacing-lg);
    }

    .form-header {
      text-align: center;
      margin-bottom: var(--spacing-lg);
      border-bottom: 2px solid var(--border-color);
      padding-bottom: var(--spacing-md);
    }

    .form-header h1 {
      font-size: 28px;
      margin-bottom: 0.5rem;
    }

    .form-header p {
      color: var(--text-light);
      font-size: 14px;
      margin: 0;
    }

    .form-group {
      margin-bottom: var(--spacing-md);
    }

    label {
      display: block;
      font-weight: 600;
      color: var(--text-dark);
      margin-bottom: 8px;
      font-size: 14px;
    }

    label.optional {
      font-weight: 500;
      color: var(--text-light);
    }

    label.optional::after {
      content: " (optional)";
      color: var(--text-lighter);
      font-size: 12px;
    }

    input[type="text"],
    input[type="email"],
    input[type="tel"],
    select {
      width: 100%;
      padding: 12px;
      border: 1px solid var(--border-color);
      border-radius: 6px;
      font-size: 14px;
      font-family: inherit;
      transition: var(--transition);
    }

    input[type="text"]:focus,
    input[type="email"]:focus,
    input[type="tel"]:focus,
    select:focus {
      outline: none;
      border-color: var(--primary-color);
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    /* Consent Section */
    .consent-section {
      background-color: var(--bg-light);
      border: 2px solid var(--border-color);
      border-radius: 6px;
      padding: var(--spacing-md);
      margin-bottom: var(--spacing-md);
    }

    .consent-section h3 {
      font-size: 16px;
      margin-bottom: 1rem;
      margin-top: 0;
    }

    .checkbox-wrapper {
      display: flex;
      align-items: flex-start;
      gap: 12px;
    }

    input[type="checkbox"] {
      margin-top: 4px;
      cursor: pointer;
      width: 18px;
      height: 18px;
      flex-shrink: 0;
    }

    .consent-text {
      flex: 1;
      font-size: 14px;
      line-height: 1.6;
      color: var(--text-medium);
    }

    .consent-text a {
      color: var(--primary-color);
      text-decoration: none;
      border-bottom: 1px solid var(--primary-color);
      font-weight: 600;
      transition: var(--transition);
    }

    .consent-text a:hover {
      color: var(--primary-dark);
      border-bottom-color: var(--primary-dark);
    }

    /* Form Actions */
    .form-actions {
      display: flex;
      gap: 12px;
      margin-top: var(--spacing-lg);
    }

    .btn-submit {
      flex: 1;
      padding: 14px;
      background: var(--gradient-primary);
      color: white;
      border: none;
      border-radius: var(--border-radius);
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .btn-submit:hover:not(:disabled) {
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(102, 126, 234, 0.4);
    }

    .btn-submit:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .btn-reset {
      padding: 14px 28px;
      background: white;
      color: var(--primary-color);
      border: 2px solid var(--primary-color);
      border-radius: var(--border-radius);
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
    }

    .btn-reset:hover {
      background: var(--bg-light);
    }

    /* Hidden - Shown on submission */
    #demoForm {
      display: block;
    }

    #confirmationScreen {
      display: none;
    }

    /* Confirmation Screen */
    .confirmation-container {
      text-align: center;
    }

    .success-icon {
      font-size: 64px;
      margin-bottom: 1rem;
    }

    .confirmation-title {
      font-size: 28px;
      margin-bottom: 0.5rem;
      color: var(--success-color);
    }

    .confirmation-subtitle {
      color: var(--text-light);
      font-size: 16px;
      margin-bottom: var(--spacing-lg);
    }

    /* Consent Record */
    .consent-record {
      background: white;
      border: 2px solid var(--success-color);
      border-radius: var(--border-radius);
      padding: var(--spacing-md);
      margin-bottom: var(--spacing-lg);
      text-align: left;
    }

    .consent-record h4 {
      color: var(--text-dark);
      margin-bottom: 1rem;
      border-bottom: 1px solid var(--border-color);
      padding-bottom: 0.5rem;
    }

    .record-item {
      display: flex;
      gap: 16px;
      margin-bottom: 12px;
      font-size: 14px;
    }

    .record-label {
      font-weight: 600;
      color: var(--text-medium);
      min-width: 100px;
    }

    .record-value {
      color: var(--text-dark);
      word-break: break-word;
    }

    /* SMS Mockup */
    .sms-mockup {
      background: white;
      border: 2px solid var(--border-color);
      border-radius: var(--border-radius);
      padding: var(--spacing-md);
      margin-bottom: var(--spacing-lg);
      text-align: left;
    }

    .sms-mockup h4 {
      margin-bottom: 1rem;
      color: var(--text-dark);
    }

    .sms-bubble {
      background: #e5e5ea;
      border-radius: 18px;
      padding: 12px 16px;
      display: inline-block;
      max-width: 90%;
      word-wrap: break-word;
      font-size: 14px;
      line-height: 1.5;
      color: var(--text-dark);
    }

    .sms-bubble a {
      color: var(--primary-color);
      text-decoration: underline;
    }

    .sms-label {
      font-size: 12px;
      color: var(--text-light);
      margin-bottom: 8px;
    }

    /* Compliance Section */
    .compliance-section {
      background: white;
      border-radius: var(--border-radius);
      padding: var(--spacing-lg);
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .compliance-section h2 {
      font-size: 22px;
      margin-bottom: 1rem;
      border-bottom: 2px solid var(--border-color);
      padding-bottom: 1rem;
    }

    .compliance-subsection {
      margin-bottom: var(--spacing-lg);
    }

    .compliance-subsection h3 {
      font-size: 16px;
      margin-bottom: 0.5rem;
      color: var(--primary-color);
    }

    .compliance-subsection p {
      color: var(--text-medium);
      font-size: 14px;
      line-height: 1.7;
      margin-bottom: 0.5rem;
    }

    .compliance-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .compliance-list li {
      padding-left: 24px;
      position: relative;
      color: var(--text-medium);
      font-size: 14px;
      line-height: 1.7;
      margin-bottom: 0.75rem;
    }

    .compliance-list li::before {
      content: "✓";
      position: absolute;
      left: 0;
      color: var(--success-color);
      font-weight: bold;
    }

    .compliance-cta {
      margin-top: var(--spacing-md);
    }

    .compliance-cta a {
      display: inline-block;
      padding: 12px 24px;
      background: var(--primary-color);
      color: white;
      border-radius: 6px;
      font-weight: 600;
      font-size: 14px;
      text-decoration: none;
      transition: var(--transition);
    }

    .compliance-cta a:hover {
      background: var(--primary-dark);
    }

    /* Validation Errors */
    .error-message {
      color: var(--error-color);
      font-size: 13px;
      margin-top: 4px;
      display: none;
    }

    .error-message.show {
      display: block;
    }

    input.error,
    select.error {
      border-color: var(--error-color);
      background-color: rgba(245, 101, 101, 0.05);
    }

    /* Responsive */
    @media (max-width: 640px) {
      .demo-content-wrapper {
        padding: 0 var(--spacing-xs);
      }

      .demo-form-card {
        padding: var(--spacing-md);
      }

      .form-header h1 {
        font-size: 22px;
      }

      .form-actions {
        flex-direction: column;
      }

      .btn-submit,
      .btn-reset {
        width: 100%;
      }

      .record-item {
        flex-direction: column;
        gap: 4px;
      }

      .compliance-section {
        padding: var(--spacing-md);
      }

      .compliance-section h2 {
        font-size: 18px;
      }
    }
  </style>
</head>
<body>

  <!-- Navigation -->
  <nav class="navbar">
    <div class="nav-container">
      <a href="/" class="logo">
        <span class="logo-text">FilterFive</span>
      </a>

      <div class="nav-links" id="navLinks">
        <a href="#how-it-works">How It Works</a>
        <a href="#benefits">Benefits</a>
        <a href="#features">Features</a>
        <a href="#pricing">Pricing</a>
        <a href="#faq">FAQ</a>
        <a href="/partners">Become a Partner</a>
        <a href="/login" class="nav-login">Login</a>
        <a href="/signup" class="btn-primary-nav">Start Free Trial</a>
      </div>

      <button class="hamburger" id="hamburger" aria-label="Toggle navigation">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>

  <div class="demo-container">
    <div class="demo-content-wrapper">

      <!-- Blue Banner -->
      <div class="demo-banner">
        <p>📋 This is a demonstration of how FilterFive-powered businesses collect SMS consent from customers. This form does not send real messages.</p>
      </div>

      <!-- Demo Form -->
      <div class="demo-form-card">
        <form id="demoForm">
          <div class="form-header">
            <h1>✂️ Joe's Barbershop</h1>
            <p>Book an Appointment</p>
          </div>

          <!-- Customer Name -->
          <div class="form-group">
            <label for="customerName">Customer Name *</label>
            <input type="text" id="customerName" name="customerName" placeholder="John Smith" required>
            <div class="error-message" id="nameError">Please enter your name</div>
          </div>

          <!-- Phone Number -->
          <div class="form-group">
            <label for="phoneNumber">Phone Number *</label>
            <input type="tel" id="phoneNumber" name="phoneNumber" placeholder="(555) 123-4567" required>
            <div class="error-message" id="phoneError">Please enter a valid phone number</div>
          </div>

          <!-- Email -->
          <div class="form-group">
            <label for="email" class="optional">Email</label>
            <input type="email" id="email" name="email" placeholder="john@example.com">
            <div class="error-message" id="emailError">Please enter a valid email address</div>
          </div>

          <!-- Service Selection -->
          <div class="form-group">
            <label for="service">Select a Service *</label>
            <select id="service" name="service" required>
              <option value="">Choose a service...</option>
              <option value="haircut">Classic Haircut</option>
              <option value="fade">Fade</option>
              <option value="beard">Beard Trim</option>
              <option value="lineup">Line Up</option>
              <option value="full-service">Full Service (Haircut + Beard)</option>
              <option value="kids">Kids Haircut</option>
            </select>
            <div class="error-message" id="serviceError">Please select a service</div>
          </div>

          <!-- Consent Checkbox -->
          <div class="consent-section">
            <h3>Feedback Consent</h3>
            <div class="checkbox-wrapper">
              <input type="checkbox" id="smsConsent" name="smsConsent" required>
              <label for="smsConsent" style="font-weight: 400; margin: 0; cursor: pointer;">
                <span class="consent-text">I agree to receive marketing messages from Joe's Barbershop, including a one-time review request after my visit. Message and data rates may apply. Reply STOP to opt out. <a href="/terms" target="_blank">Terms</a> | <a href="/privacy" target="_blank">Privacy</a></span>
              </label>
            </div>
            <div class="error-message" id="consentError">You must agree to receive an SMS review request</div>
          </div>

          <!-- Form Actions -->
          <div class="form-actions">
            <button type="submit" class="btn-submit">Book Appointment & Opt In</button>
            <button type="reset" class="btn-reset">Clear</button>
          </div>
        </form>

        <!-- Confirmation Screen -->
        <div id="confirmationScreen">
          <div class="confirmation-container">
            <div class="success-icon">✅</div>
            <h2 class="confirmation-title">Appointment Confirmed!</h2>
            <p class="confirmation-subtitle">Your appointment has been booked and your SMS consent has been recorded.</p>

            <!-- Consent Record -->
            <div class="consent-record">
              <h4>📋 Consent Record</h4>
              <div class="record-item">
                <span class="record-label">Timestamp:</span>
                <span class="record-value" id="recordTimestamp">—</span>
              </div>
              <div class="record-item">
                <span class="record-label">Phone:</span>
                <span class="record-value" id="recordPhone">—</span>
              </div>
              <div class="record-item">
                <span class="record-label">Method:</span>
                <span class="record-value">Web Form</span>
              </div>
              <div class="record-item">
                <span class="record-label">Consent Text:</span>
                <span class="record-value">I agree to receive marketing messages from Joe's Barbershop, including a one-time review request after my visit. Message and data rates may apply. Reply STOP to opt out.</span>
              </div>
            </div>

            <!-- SMS Mockup -->
            <div class="sms-mockup">
              <h4>📱 Sample SMS They'll Receive</h4>
              <div class="sms-label">Joe's Barbershop (Incoming Message)</div>
              <div class="sms-bubble">
                Hi <span id="smsName">John</span>! Thanks for visiting Joe's Barbershop. We'd love your feedback: <a href="https://g.page/r/CVASdxR-ATBCEAE/review" target="_blank">https://g.page/r/CVASdxR-ATBCEAE/review</a> Msg&data rates may apply. Reply STOP to opt out.
              </div>
            </div>

            <div class="form-actions">
              <button type="button" class="btn-reset" onclick="resetDemo()" style="flex: 1;">Start Over</button>
            </div>
          </div>
        </div>
      </div>

      <!-- Compliance Section -->
      <div class="compliance-section">
        <h2>📚 For Compliance Reviewers</h2>

        <div class="compliance-subsection">
          <h3>What This Demonstrates</h3>
          <p>This page shows how FilterFive-powered businesses collect SMS consent from customers before sending review requests. The form captures explicit consent with clear messaging about the review request, message rates, and opt-out instructions.</p>
        </div>

        <div class="compliance-subsection">
          <h3>Consent Requirements</h3>
          <ul class="compliance-list">
            <li>Businesses collect explicit written consent (checkbox) before messaging</li>
            <li>Consent language includes business identification and clear opt-out instructions</li>
            <li>Message frequency is limited to one (1) per customer per 30 days</li>
            <li>All messages include "Reply STOP to opt out" compliance language</li>
            <li>Businesses maintain records of consent (timestamp, method, consent text)</li>
            <li>Links to Terms of Service and Privacy Policy are provided during consent</li>
          </ul>
        </div>

        <div class="compliance-subsection">
          <h3>FilterFive Terms of Service</h3>
          <p>All FilterFive subscribing businesses are contractually required to:</p>
          <ul class="compliance-list">
            <li>Obtain prior express written consent before submitting phone numbers</li>
            <li>Use FilterFive-approved consent language (or substantially similar)</li>
            <li>Maintain consent records for minimum 4 years</li>
            <li>Comply with TCPA, CTIA guidelines, and carrier requirements</li>
            <li>Respect customer opt-out requests immediately</li>
          </ul>
        </div>

        <div class="compliance-subsection">
          <p style="font-size: 15px; font-weight: 500; margin-top: var(--spacing-md);">For full compliance documentation, carrier requirements, and regulatory information:</p>
          <div class="compliance-cta">
            <a href="/sms-compliance" target="_blank">View Full Compliance Documentation →</a>
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-grid">

        <div class="footer-col">
          <h4>Product</h4>
          <ul>
            <li><a href="/#how-it-works">How It Works</a></li>
            <li><a href="/#features">Features</a></li>
            <li><a href="/#pricing">Pricing</a></li>
            <li><a href="/#faq">FAQ</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4>Company</h4>
          <ul>
            <li><a href="/about">About</a></li>
            <li><a href="/contact">Contact</a></li>
            <li><a href="/partners">Agency Partners</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4>Legal</h4>
          <ul>
            <li><a href="/privacy">Privacy Policy</a></li>
            <li><a href="/terms">Terms of Service</a></li>
            <li><a href="/sms-compliance">SMS Compliance</a></li>
            <li><a href="/demo">SMS Consent Demo</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4>Support</h4>
          <ul>
            <li><a href="mailto:support@filterfive.io">support@filterfive.io</a></li>
            <li><a href="/login">Login</a></li>
            <li><a href="/signup">Sign Up</a></li>
          </ul>
        </div>

      </div>

      <div class="footer-bottom">
        <p>&copy; 2025 FilterFive. All rights reserved.</p>
      </div>
    </div>
  </footer>

  <!-- Scripts -->
  <script>
    // Form Submission Handler
    const demoForm = document.getElementById('demoForm');
    const confirmationScreen = document.getElementById('confirmationScreen');

    demoForm.addEventListener('submit', (e) => {
      e.preventDefault();

      // Clear previous errors
      clearErrors();

      // Validate form
      if (!validateForm()) {
        return;
      }

      // Get form data
      const name = document.getElementById('customerName').value;
      const phone = document.getElementById('phoneNumber').value;
      const service = document.getElementById('service').value;

      // Update confirmation screen
      const timestamp = new Date().toLocaleString('en-US', {
        year: 'numeric',
        month: '2-digit',
        day: '2-digit',
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit'
      });

      document.getElementById('recordTimestamp').textContent = timestamp;
      document.getElementById('recordPhone').textContent = phone;
      document.getElementById('smsName').textContent = name.split(' ')[0];

      // Show confirmation screen
      demoForm.style.display = 'none';
      confirmationScreen.style.display = 'block';

      // Scroll to top
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    // Form Validation
    function validateForm() {
      let isValid = true;
      const nameInput = document.getElementById('customerName');
      const phoneInput = document.getElementById('phoneNumber');
      const emailInput = document.getElementById('email');
      const serviceInput = document.getElementById('service');
      const consentInput = document.getElementById('smsConsent');

      // Name validation
      if (!nameInput.value.trim()) {
        showError('nameError', nameInput);
        isValid = false;
      }

      // Phone validation (basic)
      const phoneRegex = /^[+]?[(]?[0-9]{3}[)]?[-\s.]?[0-9]{3}[-\s.]?[0-9]{4,6}$/;
      if (!phoneInput.value.trim() || !phoneRegex.test(phoneInput.value.replace(/\D/g, ''))) {
        showError('phoneError', phoneInput);
        isValid = false;
      }

      // Email validation (if provided)
      if (emailInput.value.trim()) {
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(emailInput.value)) {
          showError('emailError', emailInput);
          isValid = false;
        }
      }

      // Service validation
      if (!serviceInput.value) {
        showError('serviceError', serviceInput);
        isValid = false;
      }

      // Consent validation
      if (!consentInput.checked) {
        showError('consentError', consentInput);
        isValid = false;
      }

      return isValid;
    }

    function showError(errorId, inputElement) {
      const errorElement = document.getElementById(errorId);
      errorElement.classList.add('show');
      inputElement.classList.add('error');
    }

    function clearErrors() {
      document.querySelectorAll('.error-message').forEach(el => {
        el.classList.remove('show');
      });
      document.querySelectorAll('input.error, select.error').forEach(el => {
        el.classList.remove('error');
      });
    }

    function resetDemo() {
      demoForm.reset();
      demoForm.style.display = 'block';
      confirmationScreen.style.display = 'none';
      clearErrors();
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    // Mobile hamburger menu (from homepage)
    const hamburger = document.getElementById('hamburger');
    const navLinks = document.getElementById('navLinks');

    hamburger.addEventListener('click', () => {
      navLinks.classList.toggle('active');
    });

    document.querySelectorAll('.nav-links a').forEach((link) => {
      link.addEventListener('click', () => {
        navLinks.classList.remove('active');
      });
    });
  </script>

</body>
</html>
