<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Thank You!' %> - <%= businessName %></title>
  <link rel="icon" type="image/png" href="/images/favicon.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #FFF9F0 0%, #FFFFFF 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 24px;
      color: #500B42;
    }
    .logo {
      margin-bottom: 32px;
    }
    .logo a {
      display: inline-block;
      text-decoration: none;
    }
    .logo img {
      height: 40px;
      width: auto;
    }
    .card {
      background: white;
      border-radius: 24px;
      padding: 48px 32px;
      text-align: center;
      box-shadow: 0 4px 24px rgba(35, 0, 30, 0.08);
      max-width: 400px;
      width: 100%;
    }
    .checkmark {
      width: 72px;
      height: 72px;
      background: #FFBA49;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px;
      animation: scaleIn 0.4s ease-out;
    }
    .checkmark svg {
      width: 36px;
      height: 36px;
      color: #500B42;
    }
    @keyframes scaleIn {
      0% { transform: scale(0); opacity: 0; }
      50% { transform: scale(1.1); }
      100% { transform: scale(1); opacity: 1; }
    }
    h1 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 8px;
      color: #500B42;
    }
    .business-name {
      font-size: 18px;
      font-weight: 600;
      color: #FFBA49;
      margin-bottom: 16px;
    }
    .message {
      font-size: 16px;
      color: #6B7280;
      line-height: 1.5;
    }
    .footer {
      margin-top: 32px;
      font-size: 12px;
      color: #9CA3AF;
    }
    .footer a {
      color: #9CA3AF;
      text-decoration: none;
    }
    .footer a:hover {
      color: #FFBA49;
    }
  </style>
</head>
<body>
  <div class="logo">
    <a href="https://morestars.io" target="_blank" rel="noopener">
      <img src="/images/Logo%20MoreStars.io.png" alt="MoreStars">
    </a>
  </div>

  <div class="card">
    <div class="checkmark">
      <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
      </svg>
    </div>
    <h1><%= title || 'Thank You' %> from</h1>
    <p class="business-name"><%= businessName %></p>
    <p class="message"><%= message || 'We appreciate your feedback!' %></p>
  </div>

  <div class="footer">
    Powered by <a href="https://morestars.io" target="_blank" rel="noopener">MoreStars</a>
  </div>
</body>
</html>
