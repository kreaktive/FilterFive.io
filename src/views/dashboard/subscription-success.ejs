<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- Google Tag Manager -->
  <script<%- typeof cspNonce !== 'undefined' ? ` nonce="${cspNonce}"` : '' %>>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
  new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
  j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
  'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
  })(window,document,'script','dataLayer','GTM-W6336VRQ');</script>
  <!-- End Google Tag Manager -->
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --brand-dark: #500B42;
      --brand-gold: #FFBA49;
      --brand-accent: #A1438E;
      --brand-positive: #20A39E;
      --brand-negative: #EF5B5B;
      --bg-light: #F9F7FA;
      --text-primary: #500B42;
      --text-secondary: #64748b;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--bg-light);
      color: var(--text-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 20px;
    }

    .success-container {
      max-width: 600px;
      width: 100%;
      background: white;
      border-radius: 16px;
      padding: 50px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    .success-icon {
      width: 100px;
      height: 100px;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 30px;
      font-size: 50px;
      animation: successPop 0.5s ease-out;
    }

    @keyframes successPop {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    h1 {
      font-size: 32px;
      color: var(--brand-dark);
      margin-bottom: 15px;
    }

    .subtitle {
      font-size: 18px;
      color: var(--text-secondary);
      margin-bottom: 30px;
      line-height: 1.6;
    }

    .info-box {
      background: #f0fdf4;
      border-left: 4px solid #10b981;
      padding: 20px;
      margin: 30px 0;
      border-radius: 8px;
      text-align: left;
    }

    .info-box h3 {
      color: #065f46;
      font-size: 18px;
      margin-bottom: 15px;
    }

    .info-row {
      display: flex;
      justify-content: space-between;
      padding: 10px 0;
      border-bottom: 1px solid #d1fae5;
    }

    .info-row:last-child {
      border-bottom: none;
    }

    .info-label {
      color: #059669;
      font-weight: 600;
    }

    .info-value {
      color: #065f46;
    }

    .btn-dashboard {
      display: inline-block;
      padding: 15px 40px;
      background: var(--brand-gold);
      color: var(--brand-dark);
      text-decoration: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      margin-top: 20px;
    }

    .btn-dashboard:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(255, 186, 73, 0.4);
    }

    .features-list {
      list-style: none;
      margin: 20px 0;
      text-align: left;
    }

    .features-list li {
      padding: 12px 0;
      color: #4a5568;
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 15px;
    }

    .features-list li::before {
      content: "✓";
      color: #10b981;
      font-weight: 700;
      font-size: 20px;
      flex-shrink: 0;
    }

    @media (max-width: 600px) {
      .success-container {
        padding: 30px 20px;
      }

      h1 {
        font-size: 26px;
      }

      .subtitle {
        font-size: 16px;
      }
    }
  </style>
</head>
<body>
  <!-- Google Tag Manager (noscript) -->
  <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-W6336VRQ"
  height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
  <!-- End Google Tag Manager (noscript) -->

  <!-- Track purchase conversion -->
  <script<%- typeof cspNonce !== 'undefined' ? ` nonce="${cspNonce}"` : '' %>>
  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({
    event: 'purchase',
    ecommerce: {
      transaction_id: '<%= session ? session.id : "" %>',
      value: <%= session ? (session.amount_total / 100).toFixed(2) : 0 %>,
      currency: 'USD',
      items: [{
        item_name: 'MoreStars Subscription',
        price: <%= session ? (session.amount_total / 100).toFixed(2) : 0 %>,
        quantity: 1
      }]
    }
  });
  </script>

  <div class="success-container">
    <div class="success-icon">✓</div>

    <h1>Subscription Activated!</h1>
    <p class="subtitle">
      Thank you, <%= businessName %>! Your MoreStars subscription is now active.
    </p>

    <div class="info-box">
      <h3>Your Subscription Details</h3>
      <div class="info-row">
        <span class="info-label">Status:</span>
        <span class="info-value">Active</span>
      </div>
      <% if (session) { %>
        <div class="info-row">
          <span class="info-label">Amount Paid:</span>
          <span class="info-value">$<%= (session.amount_total / 100).toFixed(2) %></span>
        </div>
      <% } %>
      <div class="info-row">
        <span class="info-label">SMS Limit:</span>
        <span class="info-value">1,000 messages/month</span>
      </div>
    </div>

    <div style="background: #eff6ff; border-radius: 8px; padding: 20px; margin: 20px 0;">
      <h3 style="color: #1e40af; font-size: 18px; margin-bottom: 15px;">What's Included:</h3>
      <ul class="features-list">
        <li>1,000 SMS messages per month</li>
        <li>Unlimited feedback requests</li>
        <li>Unlimited QR codes</li>
        <li>Email notifications</li>
        <li>Review filtering & capture</li>
        <li>Priority customer support</li>
      </ul>
    </div>

    <p style="color: #718096; font-size: 14px; margin: 20px 0;">
      A confirmation email has been sent to your inbox. You can manage your subscription anytime from your dashboard.
    </p>

    <a href="/dashboard" class="btn-dashboard">
      Go to Dashboard
    </a>

    <p style="margin-top: 30px; color: var(--text-secondary); font-size: 13px;">
      Need help? Contact us at <a href="mailto:support@morestars.io" style="color: var(--brand-gold);">support@morestars.io</a>
    </p>
  </div>
</body>
</html>
