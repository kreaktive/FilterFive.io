# ============================================================================
# MoreStars Backup Configuration
# ============================================================================
#
# Copy this file to .env.backup and configure for your environment
#
# cp .env.backup.example .env.backup
#
# ============================================================================

# ============================================================================
# LOCAL BACKUP SETTINGS
# ============================================================================

# Directory to store local backups
BACKUP_DIR=/var/backups/morestars

# Number of days to keep daily backups
BACKUP_RETENTION_DAYS=7

# Docker container names (match docker-compose.prod.yml)
DB_CONTAINER=morestars_db_prod
REDIS_CONTAINER=morestars_redis_prod

# ============================================================================
# AWS S3 BACKUP (Optional)
# ============================================================================
#
# To use S3 backups:
# 1. Create an S3 bucket
# 2. Create an IAM user with S3 write permissions
# 3. Install AWS CLI: curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
# 4. Configure AWS: aws configure

# S3 bucket name (leave empty to disable)
S3_BUCKET=morestars-backups

# AWS region
S3_REGION=us-east-1

# ============================================================================
# GOOGLE DRIVE BACKUP (Optional - via rclone)
# ============================================================================
#
# To use Google Drive backups:
# 1. Install rclone: curl https://rclone.org/install.sh | sudo bash
# 2. Configure rclone: rclone config
#    - New remote -> name: gdrive -> type: drive -> follow prompts
# 3. Test: rclone ls gdrive:
# 4. Create folder: rclone mkdir gdrive:morestars-backups

# Rclone remote name (leave empty to disable)
# Example: gdrive, google-drive, etc.
RCLONE_REMOTE=gdrive

# ============================================================================
# NOTIFICATIONS (Optional - Future)
# ============================================================================

# Slack webhook for backup notifications
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# Email for backup reports
# BACKUP_NOTIFY_EMAIL=admin@morestars.io
