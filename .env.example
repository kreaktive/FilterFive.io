# ============================================================================
# FilterFive Environment Configuration
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to create your local environment file:
#    cp .env.example .env
#
# 2. Replace all placeholder values with your actual credentials
#
# 3. NEVER commit the .env file to version control
#    (it's already in .gitignore)
#
# 4. For production, copy to .env.production and use strong values:
#    - Generate SESSION_SECRET: openssl rand -hex 32
#    - Generate DB_PASSWORD: openssl rand -base64 32
#
# ============================================================================

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Environment mode (development, production, test)
NODE_ENV=development

# Port the application listens on
PORT=3000

# Full public URL of your application (used in SMS links and emails)
# Development: http://localhost:3000
# Production: https://yourdomain.com
APP_URL=http://localhost:3000

# Session secret for cookie encryption (64+ characters recommended)
# Generate with: openssl rand -hex 32
SESSION_SECRET=changeme-use-long-random-string-min-64-chars

# API secret for webhook authentication (optional - for Zapier security)
API_SECRET=changeme-random-api-secret

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL connection details
DB_HOST=db
DB_PORT=5432
DB_NAME=filterfive
DB_USER=postgres
DB_PASSWORD=changeme-strong-database-password

# Docker Compose uses these for PostgreSQL container initialization
# (should match DB_* values above)
POSTGRES_DB=filterfive
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme-strong-database-password

# ============================================================================
# TWILIO (SMS DELIVERY)
# ============================================================================
# Get credentials from: https://console.twilio.com
#
# IMPORTANT: Use Twilio Messaging Service for production (A2P 10DLC compliant)
# Single phone numbers are limited and may be blocked for business use

# Your Twilio Account SID (starts with AC...)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Your Twilio Auth Token
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here

# Your Twilio phone number (for testing) or Messaging Service SID (production)
TWILIO_PHONE_NUMBER=+1XXXXXXXXXX

# Twilio Messaging Service SID (recommended for production, starts with MG...)
# Create at: https://console.twilio.com/us1/develop/sms/services
TWILIO_MESSAGING_SERVICE_SID=MGxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# RESEND (EMAIL DELIVERY)
# ============================================================================
# Get API key from: https://resend.com/api-keys
#
# IMPORTANT: Domain must be verified in Resend dashboard
# Verify at: https://resend.com/domains

# Resend API Key (starts with re_...)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# Email address to send FROM (must be from verified domain)
# Example: noreply@yourdomain.com
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ============================================================================
# GOOGLE reCAPTCHA (BOT PROTECTION FOR SIGNUP)
# ============================================================================
# Get keys from: https://www.google.com/recaptcha/admin
#
# Use reCAPTCHA v2 (Checkbox)
# Add your domain to the authorized domains list
#
# IMPORTANT: For local development, add localhost to allowed domains

# reCAPTCHA Site Key (public key, safe to expose in HTML)
# Your production site key:
RECAPTCHA_SITE_KEY=6LdUVhssAAAAAGhf26DiC2VidyBBKhSiX8fWHYTH

# reCAPTCHA Secret Key (private key, keep secure)
# Get this from: https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=your_secret_key_here

# NOTE: For local development, you can use Google's test keys:
# Site Key: 6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
# Secret Key: 6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

# ============================================================================
# STRIPE (PAYMENT PROCESSING) - OPTIONAL
# ============================================================================
# Get credentials from: https://dashboard.stripe.com/apikeys
#
# NOTE: Stripe integration is planned but not yet implemented in the codebase
# You can leave these blank for now

# Stripe Secret Key (starts with sk_test_... for test mode, sk_live_... for production)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Publishable Key (starts with pk_test_... for test mode, pk_live_... for production)
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Webhook Secret (starts with whsec_...)
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# DEVELOPMENT/TESTING ONLY
# ============================================================================

# Email address for testing email delivery (used by npm run test:email)
TEST_EMAIL=your.email@example.com

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================
# Before deploying to production, ensure:
#
# [ ] All placeholder values replaced with real credentials
# [ ] SESSION_SECRET is a strong random string (64+ characters)
# [ ] DB_PASSWORD is a strong random string (32+ characters)
# [ ] NODE_ENV=production
# [ ] APP_URL points to your production domain (https://...)
# [ ] Twilio credentials are production (not test mode)
# [ ] Resend domain is verified
# [ ] Stripe keys are live mode (when ready to accept payments)
# [ ] This .env file is NOT committed to version control
# [ ] All secrets are stored securely (password manager or secrets vault)
#
# ============================================================================
